'use strict';

/**
 * @param {number[]} nums
 * @param {number} target
 * @return {number[]}
 */
// eslint-disable-next-line no-unused-vars
function twoSum(arr, target) {
    console.log(arr, target);
}

